'use client';
/* eslint-disable @next/next/no-img-element */
import { useEffect, useMemo, useState } from 'react';
import useSWR from 'swr';
import { createHospitalSignatory, deleteHospitalSignatory, deleteHospitalSignatorySignature, getHospitalSignatories, getHospitals, updateHospitalSignatory, uploadHospitalSignatorySignature } from '@/lib/api';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { FormLabel } from '@/components/ui/form';
import { Input } from '@/components/ui/input';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

function getApiOrigin(): string | null { const base = process.env.NEXT_PUBLIC_API_BASE_URL; if (!base) return null; try { return new URL(base).origin; } catch { return null; } }
function resolveAssetUrl(url?: string | null): string | null { if (!url) return null; if (/^https?:\/\//i.test(url)) return url; const origin = getApiOrigin(); if (url.startsWith('/storage') || url.startsWith('/api')) return origin ? `${origin}${url}` : url; const base = process.env.NEXT_PUBLIC_API_BASE_URL; if (!base) return url; try { return new URL(url, base.endsWith('/') ? base : `${base}/`).toString(); } catch { return url; } }
const parseErr = async (err: any, fallback: string) => { if (err?.response?.status === 403) return 'You do not have permission to manage signatories.'; const msg = err?.response ? await err.response.json().then((r: any) => Object.values(r?.errors ?? {}).flat().join(' ')).catch(() => null) : null; return msg || fallback; };

export default function DoctorSignatorySection({ userId, userName, positionTitle }: { userId: string; userName: string; positionTitle: string }) {
  const [hospitalId, setHospitalId] = useState(''); const [msg, setMsg] = useState<string | null>(null); const [err, setErr] = useState<string | null>(null); const [form, setForm] = useState({ name: userName, position_title: positionTitle, sip_number: '', active: true }); const [file, setFile] = useState<File | null>(null); const [signatureLoadFailed, setSignatureLoadFailed] = useState(false);
  const { data: hRes, error: hErr, isLoading: hLoad } = useSWR('/hospitals', () => getHospitals());
  const { data: sRes, error: sErr, mutate, isLoading: signatoryLoading } = useSWR(hospitalId ? ['hospital-signatories', hospitalId, userId] : null, () => getHospitalSignatories(hospitalId));
  const hospitals = useMemo(() => (hRes?.data ?? []).map((h: any) => ({ id: Number(h.id), name: h.attributes?.name ?? `Hospital ${h.id}` })).filter((h: { id: number; name: string }) => Number.isFinite(h.id) && h.id > 0 && h.name.trim().length > 0), [hRes]);
  const selected = useMemo(() => (sRes?.data ?? []).find((i: any) => Number(i.attributes?.user_id) === Number(userId)) ?? null, [sRes, userId]);
  useEffect(() => { if (selected) setForm({ name: selected.attributes?.name ?? '', position_title: selected.attributes?.position_title ?? '', sip_number: selected.attributes?.sip_number ?? '', active: Boolean(selected.attributes?.active) }); else setForm({ name: userName, position_title: positionTitle, sip_number: '', active: true }); setSignatureLoadFailed(false); }, [selected, userName, positionTitle]);
  const signatureUrl = useMemo(() => resolveAssetUrl(selected?.attributes?.signature_image_url), [selected]);

  return <Card><CardHeader><CardTitle>Doctor Signature / Signatory</CardTitle></CardHeader><CardContent className='space-y-3'>{msg && <p className='text-sm text-emerald-700'>{msg}</p>}{err && <p className='text-sm text-destructive'>{err}</p>}{hLoad && <p className='text-sm text-muted-foreground'>Loading hospitals...</p>}{hErr && <p className='text-sm text-destructive'>Unable to load hospitals for signatory setup.</p>}{!hLoad && !hErr && hospitals.length === 0 && <p className='text-sm text-muted-foreground'>No hospitals available.</p>}<div className='space-y-2'><FormLabel>Hospital</FormLabel><Select value={hospitalId} onValueChange={(value) => { setErr(null); setMsg(null); setHospitalId(value); }}><SelectTrigger><SelectValue placeholder='Select hospital' /></SelectTrigger>{hospitals.length > 0 ? <SelectContent>{hospitals.map((h: { id: number; name: string }) => <SelectItem key={h.id} value={String(h.id)}>{h.name}</SelectItem>)}</SelectContent> : null}</Select></div>{hospitalId && signatoryLoading && <p className='text-sm text-muted-foreground'>Loading signatories...</p>}{hospitalId && sErr && <p className='text-sm text-destructive'>Unable to load signatories for this hospital.</p>}<Input value={form.name} onChange={(e) => setForm((p) => ({ ...p, name: e.target.value }))} placeholder='Name' /><Input value={form.position_title} onChange={(e) => setForm((p) => ({ ...p, position_title: e.target.value }))} placeholder='Position title' /><Input value={form.sip_number} onChange={(e) => setForm((p) => ({ ...p, sip_number: e.target.value }))} placeholder='SIP number' /><label className='flex gap-2 text-sm'><input type='checkbox' checked={form.active} onChange={(e) => setForm((p) => ({ ...p, active: e.target.checked }))} />Active</label><div className='flex gap-2'><Button type='button' onClick={async () => { setErr(null); setMsg(null); if (!hospitalId) return setErr('Please select a hospital first.'); if (!form.name.trim()) return setErr('Signatory name is required.'); const payload = { user_id: Number(userId), name: form.name.trim(), position_title: form.position_title, sip_number: form.sip_number, active: form.active }; try { if (selected?.id) await updateHospitalSignatory(selected.id, payload); else await createHospitalSignatory(hospitalId, payload); await mutate(); setMsg('Signatory saved successfully.'); } catch (e: any) { setErr(await parseErr(e, 'Failed to save signatory.')); if (e?.response?.status === 422) await mutate(); } }}>Save Signatory</Button>{selected && <Button type='button' variant='destructive' onClick={async () => { if (!confirm('Delete this signatory?')) return; setErr(null); setMsg(null); try { await deleteHospitalSignatory(selected.id); await mutate(); setMsg('Signatory deleted successfully.'); } catch (e: any) { setErr(await parseErr(e, 'Failed to delete signatory.')); } }}>Delete Signatory</Button>}</div>{selected ? <div className='space-y-2'>{signatureUrl && !signatureLoadFailed && <img src={signatureUrl} alt='Doctor signature' className='max-h-28 w-full object-contain' onError={() => setSignatureLoadFailed(true)} />}{signatureLoadFailed && <p className='text-sm text-destructive'>Signature image could not be loaded.</p>}<Input type='file' accept='image/png' onChange={(e) => setFile(e.target.files?.[0] ?? null)} /><div className='flex gap-2'><Button type='button' disabled={!file} onClick={async () => { if (!file) return; setErr(null); setMsg(null); const isPng = file.type === 'image/png' || (file.type === '' && file.name.toLowerCase().endsWith('.png')); if (!isPng) return setErr('Only PNG signature files are allowed.'); try { await uploadHospitalSignatorySignature(selected.id, file); await mutate(); setFile(null); setSignatureLoadFailed(false); setMsg('Signature uploaded successfully.'); } catch (e: any) { setErr(await parseErr(e, 'Failed to upload signature.')); } }}>Upload Signature</Button>{signatureUrl && <Button type='button' variant='outline' onClick={async () => { if (!confirm('Delete this signature?')) return; setErr(null); setMsg(null); try { await deleteHospitalSignatorySignature(selected.id); await mutate(); setSignatureLoadFailed(false); setMsg('Signature deleted successfully.'); } catch (e: any) { setErr(await parseErr(e, 'Failed to delete signature.')); } }}>Delete Signature</Button>}</div></div> : <p className='text-sm text-muted-foreground'>Save signatory first before uploading signature.</p>}</CardContent></Card>;
}
