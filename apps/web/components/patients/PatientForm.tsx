'use client';
import { useMemo, useState } from 'react';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { FormLabel } from '@/components/ui/form';
import { Textarea } from '@/components/ui/textarea';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { type PatientPayload } from '@/lib/api';
export type PatientFormValues = { mrn:string; name:string; gender:'male'|'female'; dob:string; dos:string; age:string; height_cm:string; weight_kg:string; bsa:string; blood_pressure:string; diagnosis_brief:string; referring_physician:string; hospital_id:string; user_id:string };
type Option={id:number; name:string; role?:string};
export const normalizePatientPayload=(v:PatientFormValues):PatientPayload=>({ mrn:v.mrn.trim(), name:v.name.trim(), gender:v.gender, dob:v.dob||null, dos:v.dos||null, age:v.age?Number(v.age):null, height_cm:v.height_cm?Number(v.height_cm):null, weight_kg:v.weight_kg?Number(v.weight_kg):null, bsa:v.bsa?Number(v.bsa):null, blood_pressure:v.blood_pressure||null, diagnosis_brief:v.diagnosis_brief||null, referring_physician:v.referring_physician||null, hospital_id:Number(v.hospital_id), user_id:Number(v.user_id)});
export default function PatientForm({initialValues,hospitals,users,onSubmit,onDelete,submitLabel='Save'}:{initialValues:PatientFormValues;hospitals:Option[];users:Option[];onSubmit:(p:PatientPayload)=>Promise<void>;onDelete?:()=>Promise<void>;submitLabel?:string;}){const [values,setValues]=useState(initialValues);const [error,setError]=useState<string|null>(null);const sortedUsers=useMemo(()=>[...users].sort((a,b)=>Number(b.role==='doctor')-Number(a.role==='doctor')),[users]);const setField=(k:keyof PatientFormValues,v:string)=>setValues((p)=>({...p,[k]:v}));async function submit(e:React.FormEvent){e.preventDefault();setError(null);if(!values.mrn.trim()||!values.name.trim()||!values.hospital_id||!values.user_id){setError('MRN, Name, Hospital, and User are required.');return;}await onSubmit(normalizePatientPayload(values)).catch((err:any)=>setError(err?.message??'Unable to save patient.'));}
return <form onSubmit={submit} className="space-y-3">{error&&<p className="text-sm text-destructive">{error}</p>}<div className="grid gap-3 md:grid-cols-2"><div className="space-y-1"><FormLabel>MRN</FormLabel><Input value={values.mrn} onChange={(e)=>setField('mrn',e.target.value)}/></div><div className="space-y-1"><FormLabel>Name</FormLabel><Input value={values.name} onChange={(e)=>setField('name',e.target.value)}/></div><div className="space-y-1"><FormLabel>Gender</FormLabel><Select value={values.gender} onValueChange={(v)=>setField('gender',v as 'male'|'female')}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="male">male</SelectItem><SelectItem value="female">female</SelectItem></SelectContent></Select></div><div className="space-y-1"><FormLabel>Date of birth</FormLabel><Input type="date" value={values.dob} onChange={(e)=>setField('dob',e.target.value)}/></div><div className="space-y-1"><FormLabel>Date of service/study</FormLabel><Input type="date" value={values.dos} onChange={(e)=>setField('dos',e.target.value)}/></div><div className="space-y-1"><FormLabel>Age</FormLabel><Input type="number" value={values.age} onChange={(e)=>setField('age',e.target.value)}/></div><div className="space-y-1"><FormLabel>Height cm</FormLabel><Input type="number" value={values.height_cm} onChange={(e)=>setField('height_cm',e.target.value)}/></div><div className="space-y-1"><FormLabel>Weight kg</FormLabel><Input type="number" value={values.weight_kg} onChange={(e)=>setField('weight_kg',e.target.value)}/></div><div className="space-y-1"><FormLabel>BSA</FormLabel><Input type="number" step="0.01" value={values.bsa} onChange={(e)=>setField('bsa',e.target.value)}/></div><div className="space-y-1"><FormLabel>Blood pressure</FormLabel><Input value={values.blood_pressure} onChange={(e)=>setField('blood_pressure',e.target.value)}/></div><div className="space-y-1"><FormLabel>Referring physician</FormLabel><Input value={values.referring_physician} onChange={(e)=>setField('referring_physician',e.target.value)}/></div><div className="space-y-1"><FormLabel>Hospital</FormLabel><Select value={values.hospital_id} onValueChange={(v)=>setField('hospital_id',v)}><SelectTrigger><SelectValue placeholder="Select hospital"/></SelectTrigger><SelectContent>{hospitals.map((h)=><SelectItem key={h.id} value={String(h.id)}>{h.name}</SelectItem>)}</SelectContent></Select></div><div className="space-y-1"><FormLabel>User / Doctor</FormLabel><Select value={values.user_id} onValueChange={(v)=>setField('user_id',v)}><SelectTrigger><SelectValue placeholder="Select user"/></SelectTrigger><SelectContent>{sortedUsers.map((u)=><SelectItem key={u.id} value={String(u.id)}>{u.name} ({u.role ?? 'user'})</SelectItem>)}</SelectContent></Select></div></div><div className="space-y-1"><FormLabel>Diagnosis brief</FormLabel><Textarea value={values.diagnosis_brief} onChange={(e)=>setField('diagnosis_brief',e.target.value)}/></div><div className="flex gap-2"><Button type="submit">{submitLabel}</Button>{onDelete&&<Button type="button" variant="destructive" onClick={onDelete}>Delete</Button>}</div></form>;}
