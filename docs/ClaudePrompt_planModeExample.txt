Act as a senior engineer for this project.

Context:
- This is a Laravel 10 (or your framework) codebase in a Git repo.
- Use existing patterns: controllers, services, repositories, tests, etc.
- Do not introduce new packages unless absolutely necessary.
- Keep BC where possible; do not quietly break existing APIs or routes.

Task:
Plan a detailed, step‑by‑step approach to implement this feature/refactor:
[replace this with your own description, e.g.,
“Add a `/api/v1/checkout` endpoint that validates cart, calculates total, and creates an order with payments and inventory checks.”]

Constraints:
- The plan must stay in **read‑only “Plan Mode”** until I explicitly say something like “approve plan” or “execute plan”.
- First, explore the codebase:
  - Identify the main files you will need to touch.
  - Note any existing services, routes, or middleware that affect this feature.
  - Highlight any open questions you need me to answer before implementing.
- Then output a numbered plan including:
  1. A short high‑level design (where the logic lives).
  2. A list of files to read/modify.
  3. Concrete steps grouped by phase:
     - Phase 1: Setup (e.g., new routes, stubs).
     - Phase 2: Core logic (e.g., controller → service → helpers).
     - Phase 3: Tests (unit + feature).
     - Phase 4: Cleanup/refactoring (e.g., extract code, rename).
  4. For each step, specify:
     - Which file(s) are involved.
     - Approximate boundaries (what must not change, e.g., route names, DB schema).
  5. Highlight any risks or assumptions (e.g., race conditions, DB locks).

Question‑first mode:
- If you are unsure about the architecture, naming, or whether something should be in a service vs. controller, ASK me explicitly BEFORE you propose a step.
- If you find conflicting code patterns, tell me and suggest a single preferred pattern.

Output rules:
- First output the **full plan** (steps + files + constraints).
- Then stop and wait for me to:
  - Approve, edit, or ask you to refine the plan.
  - Or say “execute plan” when you are allowed to start making edits.
